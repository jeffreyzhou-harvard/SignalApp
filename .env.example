# ─────────────── Frontend (Next.js app, repo root) ───────────────
# xAI — required for chat + image generation
XAI_API_KEY=

# Provider selection (pluggable — see src/lib/providers/registry.ts)
AI_TEXT_PROVIDER=grok
AI_IMAGE_PROVIDER=grok-imagine

# Model overrides (optional)
XAI_TEXT_MODEL=grok-4.5
XAI_IMAGE_MODEL=grok-imagine-image-quality
XAI_VIDEO_MODEL=grok-imagine-video-1.5

# Video provider (pluggable — see src/lib/providers/registry.ts)
AI_VIDEO_PROVIDER=grok-imagine-video

# Storage adapter (pluggable — see src/lib/storage/index.ts)
STORAGE_ADAPTER=json

# Sign in with X (OAuth 2.0 + PKCE). Setting X_OAUTH_CLIENT_ID switches account
# linking from the local stub to the real redirect flow automatically.
# Portal: User authentication settings → Web App → callback <origin>/api/auth/x/callback
X_OAUTH_CLIENT_ID=
# Only for confidential clients; leave empty for public (PKCE-only) clients
X_OAUTH_CLIENT_SECRET=
# Optional override; defaults to <origin>/api/auth/x/callback
X_OAUTH_REDIRECT_URI=
# Force a specific account provider (x-stub | x-oauth); default is automatic
ACCOUNT_PROVIDER=

# Deploy analytics: minimum seconds between live X metric refreshes (per load).
# Higher = fewer X API reads (cheaper, rate-limit friendly); default 300 (5 min).
METRICS_TTL_SECONDS=300

# Audience data (pluggable — see src/lib/audience/registry.ts).
# "db" = real clustered audience from the ingestion backend (falls back to the
# seeded mock when the backend is down or has no active run); "mock" = sample tribes.
AUDIENCE_PROVIDER=db
# Ingestion backend base URL (FastAPI, backend/): uv run uvicorn app.main:app
BACKEND_URL=http://localhost:8000

# Wind-tunnel simulation (pluggable — see src/lib/simulation/registry.ts; "mock-agents" = seeded sample loop)
SIMULATION_PROVIDER=mock-agents

# Analytics (GoatCounter — unique visitors, no cookies/PII). Unset uses the
# project's own endpoint; set to empty to disable counting entirely.
NEXT_PUBLIC_GOATCOUNTER_URL=https://adityabilawar.goatcounter.com/count

# ─────────────── Voice harness (Grok Voice Agent API) ───────────────
# Realtime model + voice for the speech-to-speech session
XAI_VOICE_MODEL=grok-voice-latest
XAI_VOICE=eve
# Teammates' pgvector-on-Neon MCP (Streaming HTTP/SSE). Unset = tool omitted,
# the copilot still works from the tribe catalog inlined in its instructions.
AUDIENCE_MCP_URL=
AUDIENCE_MCP_TOKEN=
# Publishing goes through /api/publish (the Ship button's path): it requires a
# linked X account with OAuth write scope, and the copilot's post_to_x tool is
# draft-first — it only calls /api/publish after an explicit confirmation.

# ─────────────── Backend (data ingestion, /backend) ───────────────
# X platform read path — app-only bearer works for user lookup, followers, timelines.
# (Co-engagement likers/retweeters need OAuth user-context; degrades gracefully without it.)
X_AI_CONSUMER_KEY=
X_AI_SECRET_KEY=
X_AI_BEARER_TOKEN=
# Grok summarization key — accepted as X_AI_API_KEY or XAI_API_KEY
X_AI_API_KEY=
# Postgres: local docker OR shared Neon (postgresql+psycopg://USER:PASS@HOST/db?sslmode=require)
DATABASE_URL=postgresql+psycopg://agentsim:agentsim@localhost:5432/agentsim
# Budget guard (USD)
X_API_BUDGET_USD=250
X_API_SPEND_SOFT_LIMIT_USD=200
# Grok summarization client (model id is billing-sensitive — only grok-4.3 for v1)
GROK_MODEL=grok-4.3
GROK_BASE_URL=https://api.x.ai/v1
GROK_MAX_RETRIES=2
# API throttling / robustness
HTTP_TIMEOUT_S=30
WORKER_POLL_INTERVAL_S=1.0
FOLLOWERS_PAGE_SIZE=1000
SEED_POSTS_LOOKBACK=25
ENRICH_CONCURRENCY=8          # parallel Grok persona-card calls during tier-2 enrich
# MCP transport: public hosts allowed through DNS-rebinding protection (localhost
# always trusted). Set to your cloudflare tunnel / deploy domain. Comma-separated.
MCP_ALLOWED_HOSTS=

# Private beta: set to 1 on public deployments to lock everything except
# the landing page and /waitlist (app pages redirect, APIs return 403).
BETA_LOCK=
